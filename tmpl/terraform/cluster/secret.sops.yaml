kind: Secret
proxmox_ip: ${BOOTSTRAP_PROXMOX_IP}
proxmox_url: ${BOOTSTRAP_PROXMOX_URL}
clone_template_name: ${BOOTSTRAP_CLONE_TEMPLATE_NAME}
proxmox_vm_user: ${BOOTSTRAP_PROXMOX_VM_USER}
proxmox_vm_password: ${BOOTSTRAP_PROXMOX_VM_password}
ssh_pub_key_file: ${BOOTSTRAP_SSH_PUB_KEY_FILE}
proxmox_node: ${BOOTSTRAP_PROXMOX_NODE}
proxmox_api_token_id: ${BOOTSTRAP_PROXMOX_API_TOKEN_ID}
proxmox_api_token_secret: ${BOOTSTRAP_PROXMOX_API_TOKEN_SECRET}
master_count: ${BOOTSTRAP_MASTER_COUNT}
master_name_prefix: ${BOOTSTRAP_MASTER_NAME_PREFIX}
master_vmid_prefix: ${BOOTSTRAP_MASTER_VMID_PREFIX}
master_storage: ${BOOTSTRAP_MASTER_STORAGE}
master_ip: ${BOOTSTRAP_MASTER_IP}
master_subnet_mask: ${BOOTSTRAP_MASTER_SUBNET_MASK}
master_gateway: ${BOOTSTRAP_MASTER_GATEWAY}
node_count: ${BOOTSTRAP_NODE_COUNT}
node_name_prefix: ${BOOTSTRAP_NODE_NAME_PREFIX}
node_vmid_prefix: ${BOOTSTRAP_NODE_VMID_PREFIX}
node_storage: ${BOOTSTRAP_NODE_STORAGE}
node_ip: ${BOOTSTRAP_NODE_IP}
node_subnet_mask: ${BOOTSTRAP_NODE_SUBNET_MASK}
node_gateway: ${BOOTSTRAP_NODE_GATEWAY}
